version: '3.8'

services:
  backend:
    build: .
    container_name: potholeguard_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python scripts/run_backend.py
